spring:
  #  datasource,包含用户名，密码，url，Driver 可以连接到数据库
  datasource:
    url: jdbc:mysql://localhost:3306/db_test
    username: root
    password: Davy2018
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect #newer one,MySQL8Dialect

