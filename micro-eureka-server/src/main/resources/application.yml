server:
  port: 10086 # 服务端口

spring:
  application:
    name: eurekaserver #eureka的微服务名称，为了做服务注册
eureka:
      client:
        service-url: # eureka的地址信息，Eureka自己也是微服务，为了做服务注册
          defaultZone: http://127.0.0.1:10086/eureka
